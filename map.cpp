#include <bits/stdc++.h>

using namespace std;

int main(){
    
    // roma = 5

    // jojo = 15

    // kuka = 100
    map<string, int> mapa;

    mapa["roma"] = 5;
    mapa["jojo"] = 15;
    mapa["kuka"] = 100;

    // if(mapa.find("batyr") != mapa.end()){
    //     cout << "YES";
    // } else{
    //     cout << "NO";
    // }

    // mapa["roma"] = 15;

    // mapa.find("roma") != mapa.end()

    // mapa.erase("roma");

    // // cout << mapa["kuka"];
    // map<string, int> :: iterator it;

    // for(it = mapa.begin(); it != mapa.end(); it++){
    //     // cout << (*it).first << " " << (*it).second << endl;

    //     cout << it->first << " " << it->second << endl;
    // }


    return 0;
}