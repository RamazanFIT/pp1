#include <bits/stdc++.h>

using namespace std;

int main(){
    
    //  • Функции find, substr, getline, stoi

    // stoi -> string to integer 

    // string s = "15";

    // cout << stoi(s) / 3;

    // string s = "ramazan";

    // cout << s.find("rama");
    // s.substr()

    // sort, reverse, tolower, toupper, isdigit, sqrt, max, min

    // char a = 'h';

    // // cout << toupper(a);
    // int ascii = toupper(a);

    // cout << char(ascii);

    // char a;
    // cin >> a;
    // cout << char(tolower(a));
    // cout << char(toupper(a));
    // char a;
    // cin >> a;

    // if(a >= '0' and a <= '9'){
    //     cout << "YEs";
    // } else{
    //     cout << "No";
    // }


    // int n;
    // cin >> n;
    // int massive[n];
    // for(int i = 0; i < n; i++){
    //     cin >> massive[i];
    // }

    // // sort(massive + 0, massive + n);
    // // reverse(massive, massive + n);

    // for(int i = 0; i < n; i++){
    //     cout << massive[i] <<  " ";
    // }
    //  43 563 123

    // int a = 43;
    // int b = 563;
    // int c = 123;
    // // cout << max(a, max(b, c));
    // cout << min(a, min(b, c));



    // int maximum = INT_MIN;
    // int minimum = INT_MAX;

    // if(maximum < a){
    //     maximum = a;
    // }if(maximum < b){
    //     maximum = b;
    // }if(maximum < c){
    //     maximum = c;
    // }


    
    return 0;
}