#include <bits/stdc++.h>

using namespace std;

int main(){
    
    // string s = "ramazan";
    // nazamar
    // char massive[7];
    // s[1] = 'l';
    // cout << s[1] << endl;
    // cout << s;

    // for - string = size
    // cout << s.size();
    // for(int i = 0; i < s.size(); i++){
    //     cout << s[i] << endl;
    // }
    // for(int i = s.size() - 1; i >= 0; i--){
    //     cout << s[i];
    // }

    // reverse(s.begin(), s.end());

    // cout << s;

    // string s = "dilnazsatybaldy";
    // // // sat
    // // // if(s.find("sat"))
    // // // cout << s.find("sat"); // 6
    // // int position = s.find("sat1");
    // // cout << position; // 6
    
    // sort(s.begin(), s.end());
    // reverse(s.begin(), s.end());
    // cout << s;

    int massive[5] = {321, 22, 3, 41, 5};
    // sort(massive, massive + 5);
    // reverse(massive, massive + 5);
    for(int i = 0; i < 5; i++){
        cout << massive[i] << " ";
    }

    return 0;
}