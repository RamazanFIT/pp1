#include <bits/stdc++.h>

using namespace std;

int main(){
    
    // ramazan 

    // string s = "1234567";

    // cout << s[1];

    // string s;
    // // cin >> s;

    // getline(cin, s);

    // cout << s;

    // string s = "ramazan";

    // sort(s.begin(), s.end());
    // reverse(s.begin(), s.end());

    // cout << s;

    // cout << s.size();

    // char a = 'a'; // 97
    // char b = 'b'; // 98
    // char c = 'c'; // 99

    // // 5
    // int n;
    // cin >> n;

    // cout << char(97 + n - 1);

    // char a = 'a';   
    // a = toupper(a);
    // cout << a;
    // cout << char(toupper(a));

    // char a = 'a';
    // cout << char(tolower(a));

    // 'a' = 97 

    // 'A' = 65 

    // cout << char('a' - 32);

    // cout << char('A' + 32); 

    // string s = "ramaZAN";

    // for(int i = 0; i < s.size(); i++){
        // s[i] = char(toupper(s[i]));
    //     if(s[i] >= 'a' and s[i] <= 'z'){

    //         s[i] -= 32;
    //     }
    // }

    // cout << s;

    // string s = "ramazan";

    // cout << s.find("fofo");

    // int a = s.find("fofo");

    // cout << a;

    // char a = 97;

    // cout << a;

    // a b 

    // roma 

    // romazan 

    // YES 


    string a, b;
    cin >> a >> b;

    if(b.find(a) != -1){
        cout << "YES";
    } else{
        cout << "NO";
    }




    return 0;
}